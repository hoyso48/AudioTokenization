defaults:
  - preprocess: default
  - dataset: default
  - model: base
  - train: default
name: squeezeformer-stft-istft-1024-ls-600k-VQ16384-100hz-conv50hz-unmerge-2stage
log_dir: pl_log
debug: false
resume_ckpt: null
wandb_id: null
ckpt: null # infer
input_dir: null # infer
output_dir: null # infer
hydra:
  output_subdir: hydra
  job:
    chdir: True